import 'package:flutter/material.dart';

class AchatsScreen extends StatelessWidget {
  const AchatsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Achats — À implémenter'));
  }
}
